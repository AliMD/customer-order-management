const config = {
  hostname: '0.0.0.0',
  port: 8080,
  appIndex: './',
  rootDir: '.',
  nodeResolve: true,
  moduleDirs: ['node_modules'],
  watch: true,
  preserveSymlinks: true,
};

export default config;
